import React from "react";
import { useSelector, useDispatch } from "react-redux";

type Props = {};

export default function useModalHook({}: Props) {
  return (
    <div>
      <div></div>
    </div>
  );
}
